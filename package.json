{
  "name": "random-map-app",
  "version": "1.2.0",
  "main": "src/app/bundle/index.js",
  "scripts": {
    "build": "(cd src/ && npm run build)",
    "dev": "(cd src/ && npm run build-dev) && (DEBUG=1 nw src/ --user-data-dir=../random-map-app-user-data/)",
    "dev-nodemon": "nodemon --watch src/app/ --exec npm run dev",
    "dev-no-build": "nw src/ --user-data-dir=../random-map-app-user-data/",
    "prod-with-build": "(cd src/ && npm run build) && (nwbuild --platforms osx64 --srcDir src/* --buildDir dist/* src/* --flavor normal --cacheDir cache/ --version 0.52.2)",
    "prod": "nwbuild --platforms osx64 --srcDir src/ --buildDir dist/ src/ --flavor normal --cacheDir cache/ --version 0.52.2"
  },
  "keywords": [],
  "author": "Ukkiez",
  "license": "ISC",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "nodemon": "^3.1.14",
    "nw": "0.52.2-sdk",
    "nw-builder": "3.5.7",
    "yaml": "^2.8.0"
  },
  "nodemonConfig": {
    "ignore": ["src/app/bundle/**"]
  }
}
